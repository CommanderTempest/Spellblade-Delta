extends CharacterBody3D
class_name InteractableEntity
signal toggle_conversation()

func player_interact(player: Player) -> void:
	# going to need to pass in some dialogue, maybe fetch from a JSON in here
	#toggle_conversation.emit()
	#player.dialogue_box.visible = true
	#player.dialogue.visible = true
	pass
